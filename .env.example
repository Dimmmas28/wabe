# WABE Environment Configuration
# Copy this file to .env and fill in your API keys

# Google API Key
# Required for white agent (Gemini 2.0 Flash model)
# Get your API key at: https://aistudio.google.com/app/apikey
# Free tier: 15 requests/minute, 1500 requests/day
GOOGLE_API_KEY=your_google_api_key_here

# Logging Configuration
# Directory for log files
# LOG_DIR=.logs

# Debug Logging
# Save full HTML to files for each step (useful for debugging agent decisions)
# SAVE_DEBUG_HTML=false

# Save parsed responses to JSON files for each step (useful for debugging agent reasoning)
# SAVE_DEBUG_RESPONSES=false

# MCP Configuration
# The MCP (Model Context Protocol) server is automatically managed by the browser agent.
# No additional configuration required - the @playwright/mcp server is launched
# as a subprocess and communicates via JSON-RPC over stdin/stdout.
