# First time setup or after pulling changes:

uv sync

# To run scripts:

uv run serve_ga.py
uv run serve_wa.py
uv run main.py

# Set up PlayWright and install the browser kernels.

playwright install