[green_agent]
endpoint = "http://127.0.0.1:9009"
cmd = "uv run python scenarios/web_browser/browser_judge.py --host 127.0.0.1 --port 9009"

[[participants]]
role = "white_agent"
endpoint = "http://127.0.0.1:9019"
cmd = "uv run python -m scenarios.web_browser.agents.langgraph --host 127.0.0.1 --port 9019"

[config]
max_steps = 10

[[tasks]]
task_id = "langgraph_test_1"
website = "https://www.stubhub.com/"
task = "Show theatre events for Las Vegas and select one."
level = "easy"
